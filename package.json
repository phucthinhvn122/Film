{
  "name": "bio-website",
  "version": "1.0.0",
  "private": true,
  "description": "DevThinhPhim static app + image proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
